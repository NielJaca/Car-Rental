# Server (Render sets PORT automatically)
PORT=3000

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/car-rental?retryWrites=true&w=majority

# Session: use a long random string in production (e.g. openssl rand -hex 32)
SESSION_SECRET=your-session-secret-change-in-production

# Frontend URL for CORS and cookies. In production set to your frontend URL (e.g. https://your-app.onrender.com).
# For multiple origins use comma-separated values.
FRONTEND_URL=http://localhost:5500

# Optional: set to production for session cookie SameSite=None and MongoDB session store
NODE_ENV=development
