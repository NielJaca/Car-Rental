PORT=3000

# MongoDB Atlas connection string (REQUIRED)
# 1. Go to https://cloud.mongodb.com → Your Cluster → Connect → Drivers
# 2. Copy the connection string
# 3. Replace <password> with your database user password
# 4. URL-encode special chars in password: @ → %40, # → %23, : → %3A, / → %2F, % → %25
# Use mongodb+srv:// for Atlas (recommended)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/car-rental?retryWrites=true&w=majority

# Session secret (change in production)
SESSION_SECRET=your-session-secret-change-in-production

# Frontend URL for CORS (use your deployed frontend URL in production)
FRONTEND_URL=http://localhost:5500
